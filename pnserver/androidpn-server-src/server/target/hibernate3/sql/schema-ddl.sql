
    drop table apn_user if exists;

    create table apn_user (
        id bigint generated by default as identity (start with 1),
        created_date timestamp,
        email varchar(64),
        name varchar(64),
        password varchar(64),
        updated_date timestamp,
        username varchar(64) not null,
        primary key (id),
        unique (username)
    );
